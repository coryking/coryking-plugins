[project]
name = "cc-explorer"
version = "1.43.0"
description = "MCP server for exploring Claude Code and Codex transcripts — search conversations, quote moments, inspect subagents."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13",
    "fastmcp>=3.4.2",
    "cachetools>=7.1",
    "orjson>=3.11",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[project.scripts]
cc-explorer = "cc_explorer.mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
